# MoodSync Client (React + Vite)

## Setup
1. Install deps: `npm install`
2. Create `.env` with `VITE_API_BASE=http://localhost:5000`
3. Run dev: `npm run dev` (http://localhost:5173)

Pages:
- Home (mood quick-pick)
- Mood (text sentiment → mood)
- Playlists (shows playlists from backend)
- Study Tips
- About
- Team
- Contact
- Feedback (posts to backend)
- Login, Register (UI only)
